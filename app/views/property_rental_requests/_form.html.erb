<%= form_for(@property_rental_request) do |f| %>
  <% if @property_rental_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@property_rental_request.errors.count, "error") %> prohibited this property_rental_request from being saved:</h2>

      <ul>
      <% @property_rental_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
